TEAM_ID=
BUNDLE_ID=com.suw1labs.worktracker
APP_NAME=WorkTracker
// Xcode Cloud overwrites both from the release tag (ci_scripts/ci_pre_xcodebuild.sh). They only
// matter for a build archived by hand, which must still be higher than the last approved version.
CURRENT_PROJECT_VERSION=1
MARKETING_VERSION=1.0.12
